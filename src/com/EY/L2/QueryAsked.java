package com.EY.L2;

public class QueryAsked {
    /*
    the query was asked :

    Sql queries a table two identical table employee and both are having identical column one is 10 and other is having 5 record write the query to find the remaining record
    Select e1.*
from Employee1 e1
left join Employee2 e2
on e1.Emp_id == e2.emp_id
where e2.emp_id is null;

if
     */


}
